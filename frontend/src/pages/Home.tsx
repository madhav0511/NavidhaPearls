import { useMemo, useState } from "react";
import { toast } from "sonner";
import { ArrowDown, ArrowUpRight, ChevronDown, Gem, Menu, Search, ShieldCheck, Sparkles, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Toaster } from "@/components/ui/sonner";
import CartDrawer, { type CartItem } from "@/components/CartDrawer";
import NavidhaLogo from "@/components/NavidhaLogo";
import ProductCard from "@/components/ProductCard";
import { craftStories, formatPrice, products, type Category, type Material, type Product } from "@/data/catalog";

const categories: Array<"All" | Category> = ["All", "Pendants", "Necklaces", "Earrings", "Rings", "Bracelets"];
const materials: Array<"All materials" | Material> = ["All materials", "925 Silver", "Pearls", "Champagne Gold"];

export default function Home() {
  const [activeCategory, setActiveCategory] = useState<"All" | Category>("All");
  const [activeMaterial, setActiveMaterial] = useState<"All materials" | Material>("All materials");
  const [search, setSearch] = useState("");
  const [sort, setSort] = useState("featured");
  const [selectedProduct, setSelectedProduct] = useState<Product | null>(null);
  const [cartItems, setCartItems] = useState<CartItem[]>([]);
  const [cartOpen, setCartOpen] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [launchPopupOpen, setLaunchPopupOpen] = useState(true);

  const visibleProducts = useMemo(() => {
    const query = search.trim().toLowerCase();
    const filtered = products.filter((product) => {
      const categoryMatch = activeCategory === "All" || product.category === activeCategory;
      const materialMatch = activeMaterial === "All materials" || product.material === activeMaterial;
      const searchMatch = !query || `${product.name} ${product.category} ${product.material}`.toLowerCase().includes(query);
      return categoryMatch && materialMatch && searchMatch;
    });
    return [...filtered].sort((a, b) => {
      if (sort === "price-low") return a.price - b.price;
      if (sort === "price-high") return b.price - a.price;
      return products.indexOf(a) - products.indexOf(b);
    });
  }, [activeCategory, activeMaterial, search, sort]);

  const cartCount = cartItems.reduce((total, item) => total + item.quantity, 0);
  const addToCart = (product: Product) => {
    setCartItems((items) => {
      const existing = items.find((item) => item.product.id === product.id);
      if (existing) return items.map((item) => item.product.id === product.id ? { ...item, quantity: item.quantity + 1 } : item);
      return [...items, { product, quantity: 1 }];
    });
    toast.success(`${product.name} added to your bag`, { description: "A considered piece, reserved for your edit." });
  };
  const changeQuantity = (id: string, quantity: number) => setCartItems((items) => quantity < 1 ? items.filter((item) => item.product.id !== id) : items.map((item) => item.product.id === id ? { ...item, quantity } : item));
  const removeItem = (id: string) => setCartItems((items) => items.filter((item) => item.product.id !== id));
  const scrollToCatalog = () => document.getElementById("collection")?.scrollIntoView({ behavior: "smooth" });

  return (
    <div className="min-h-screen bg-[#fbf9f5] text-[#14202e]" data-testid="navidha-storefront">
      <Toaster richColors={false} />
      {launchPopupOpen && <div className="launch-popup-overlay" role="dialog" aria-modal="true" aria-label="Navidha launch announcement" data-testid="launch-popup"><div className="launch-popup-card"><button type="button" onClick={() => setLaunchPopupOpen(false)} className="icon-button launch-popup-close" aria-label="Close launch announcement" data-testid="launch-popup-close-button"><X size={18} /></button><div className="launch-popup-decoration" aria-hidden="true"><Sparkles size={18} /><span>Navidha · 2024</span><Sparkles size={18} /></div><p className="eyebrow text-[#9a7a3e]" data-testid="launch-popup-eyebrow">Navidha Pearls & Jewelry</p><h2 className="mt-4 font-serif text-5xl leading-[.95] tracking-[-.04em] text-[#14202e] sm:text-6xl" data-testid="launch-popup-title">We will be<br /><em className="text-[#9a7a3e]">Live Soon</em></h2><p className="mx-auto mt-5 max-w-xs text-sm leading-6 text-[#667383]" data-testid="launch-popup-description">A new chapter in Indian jewelry is almost here. Explore the preview while we prepare the collection.</p><button type="button" onClick={() => { setLaunchPopupOpen(false); window.setTimeout(scrollToCatalog, 80); }} className="mt-7 inline-flex h-11 items-center justify-center gap-3 bg-[#14202e] px-7 text-[10px] uppercase tracking-[.2em] text-[#f8f1e4] transition-colors hover:bg-[#c8a45d] hover:text-[#14202e]" data-testid="launch-popup-explore-button">Explore the preview <ArrowUpRight size={14} /></button><p className="mt-5 text-[9px] uppercase tracking-[.18em] text-[#9a7a3e]" data-testid="launch-popup-note">The story begins soon</p></div></div>}
      <div className="announcement-bar" data-testid="announcement-bar">Complimentary delivery across India <span>·</span> The Navidha collection launches soon</div>
      <header className="sticky top-0 z-40 border-b border-white/10 bg-[#14202e]/95 text-[#f8f1e4] backdrop-blur-md" data-testid="navbar">
        <div className="mx-auto flex h-[76px] max-w-[1440px] items-center justify-between px-5 sm:px-8 lg:px-16">
          <a href="#top" aria-label="Navidha home" data-testid="navbar-home-link"><NavidhaLogo /></a>
          <nav className="hidden items-center gap-8 lg:flex" aria-label="Main navigation" data-testid="desktop-navigation">
            <a href="#collection" className="nav-link" data-testid="navbar-collection-link">The collection</a>
            <a href="#craft" className="nav-link" data-testid="navbar-craft-link">Craft heritage</a>
            <a href="#philosophy" className="nav-link" data-testid="navbar-philosophy-link">Our philosophy</a>
          </nav>
          <div className="flex items-center gap-2 sm:gap-4">
            <button type="button" className="hidden text-[#c8a45d] transition-colors hover:text-[#f8f1e4] sm:block" aria-label="Search the collection" onClick={scrollToCatalog} data-testid="navbar-search-button"><Search size={18} strokeWidth={1.5} /></button>
            <button type="button" onClick={() => setCartOpen(true)} className="relative flex items-center gap-2 text-[10px] uppercase tracking-[0.18em] transition-colors hover:text-[#c8a45d]" aria-label={`Open shopping bag with ${cartCount} items`} data-testid="navbar-cart-button"><span className="hidden sm:inline">Bag</span><span className="grid h-7 min-w-7 place-items-center rounded-full border border-[#c8a45d]/50 px-1 text-[10px] text-[#c8a45d]" data-testid="navbar-cart-count">{cartCount}</span></button>
            <button type="button" onClick={() => setMobileMenuOpen((open) => !open)} className="grid h-9 w-9 place-items-center lg:hidden" aria-label="Toggle navigation" data-testid="navbar-mobile-menu-button">{mobileMenuOpen ? <X size={20} /> : <Menu size={20} />}</button>
          </div>
        </div>
        {mobileMenuOpen && <nav className="border-t border-white/10 bg-[#14202e] px-5 py-5 lg:hidden" aria-label="Mobile navigation" data-testid="mobile-navigation"><div className="flex flex-col gap-4"><a href="#collection" onClick={() => setMobileMenuOpen(false)} className="nav-link" data-testid="mobile-collection-link">The collection</a><a href="#craft" onClick={() => setMobileMenuOpen(false)} className="nav-link" data-testid="mobile-craft-link">Craft heritage</a><a href="#philosophy" onClick={() => setMobileMenuOpen(false)} className="nav-link" data-testid="mobile-philosophy-link">Our philosophy</a></div></nav>}
      </header>

      <main id="top">
        <section className="hero-section" data-testid="hero-section"><div className="hero-copy"><p className="eyebrow text-[#c8a45d]" data-testid="hero-eyebrow">Navidha Pearls & Jewelry · Est. India</p><h1 className="mt-7 max-w-[680px] font-serif text-5xl leading-[0.98] tracking-[-0.04em] text-[#f8f1e4] sm:text-7xl lg:text-[7rem]" data-testid="hero-title">Jewelry,<br /><em className="text-[#c8a45d]">reimagined.</em></h1><p className="mt-8 max-w-[450px] text-base leading-7 text-[#b8c0c8] sm:text-lg" data-testid="hero-description">Where India&apos;s timeless craftsmanship meets contemporary form. A considered edit of silver, pearls, gold and the hands that make them.</p><div className="mt-10 flex flex-wrap items-center gap-5"><Button type="button" onClick={scrollToCatalog} className="h-12 rounded-none bg-[#c8a45d] px-7 font-sans text-[10px] uppercase tracking-[0.2em] text-[#14202e] hover:bg-[#f1dfb8]" data-testid="hero-explore-button">Explore the collection <ArrowUpRight size={15} /></Button><a href="#craft" className="inline-flex items-center gap-2 text-[10px] uppercase tracking-[0.2em] text-[#f8f1e4] transition-colors hover:text-[#c8a45d]" data-testid="hero-craft-link">The craft stories <ArrowDown size={14} /></a></div><div className="mt-16 grid max-w-[430px] grid-cols-3 gap-6 border-t border-white/15 pt-5 text-[#b8c0c8]" data-testid="hero-proof-points"><div data-testid="hero-proof-silver"><strong className="block font-serif text-2xl text-[#f8f1e4]">925</strong><span className="mt-1 block text-[9px] uppercase tracking-[0.15em]">Silver standard</span></div><div data-testid="hero-proof-handcrafted"><strong className="block font-serif text-2xl text-[#f8f1e4]">100%</strong><span className="mt-1 block text-[9px] uppercase tracking-[0.15em]">Handcrafted</span></div><div data-testid="hero-proof-india"><strong className="block font-serif text-2xl text-[#f8f1e4]">∞</strong><span className="mt-1 block text-[9px] uppercase tracking-[0.15em]">Made in India</span></div></div></div><div className="hero-image-wrap" data-testid="hero-image-panel"><img src="https://images.unsplash.com/photo-1613966561243-c6959a886009?auto=format&fit=crop&w=1400&q=84" alt="Editorial arrangement of Navidha jewelry" className="hero-image" data-testid="hero-image" /><div className="hero-image-caption"><span>01</span><span className="h-px flex-1 bg-[#f8f1e4]/60" /><span>Golden light</span></div><div className="hero-stamp" aria-hidden="true"><Sparkles size={15} /><span>Navidha<br />2024</span></div></div></section>

        <section className="border-b border-[#14202e]/10 bg-[#fbf9f5] px-5 py-14 sm:px-8 lg:px-16" data-testid="manifesto-section"><div className="mx-auto grid max-w-[1440px] gap-8 lg:grid-cols-[1.1fr_1fr_1fr] lg:items-end"><div><p className="eyebrow text-[#9a7a3e]" data-testid="manifesto-eyebrow">A quieter kind of luxury</p><h2 className="mt-4 max-w-xl font-serif text-4xl leading-tight tracking-[-0.03em] sm:text-5xl" data-testid="manifesto-title">Made for the moments that become <em className="text-[#9a7a3e]">yours.</em></h2></div><p className="max-w-sm text-sm leading-7 text-[#667383]" data-testid="manifesto-copy">Navidha brings together India&apos;s extraordinary jewelry traditions and contemporary design — pieces chosen for their character, made to gather meaning with you.</p><div className="flex items-center gap-4 lg:justify-end" data-testid="manifesto-materials"><div className="grid h-12 w-12 place-items-center rounded-full border border-[#c8a45d]/40 text-[#c8a45d]"><Gem size={20} strokeWidth={1.2} /></div><div><p className="font-serif text-xl">Gold. Silver. Pearl.</p><p className="text-xs text-[#667383]">Every piece has a story.</p></div></div></div></section>

        <section id="craft" className="section-shell bg-[#f0ebe3]" data-testid="heritage-craft-section"><div className="section-heading"><div><p className="eyebrow text-[#9a7a3e]" data-testid="craft-eyebrow">Heritage, curated</p><h2 className="section-title" data-testid="craft-title">The hands behind<br /><em>the beauty.</em></h2></div><p className="section-intro" data-testid="craft-intro">From colored glass gold and silver jewelry to delicate silver filigree, we honour the traditions that make Indian jewelry unmistakably alive.</p></div><div className="craft-grid">{craftStories.map((story, index) => <article key={story.id} className={`craft-card ${index === 0 ? "craft-card-featured" : ""}`} data-testid={`craft-card-${story.id}`}><img src={story.image} alt={`${story.name} jewelry craft`} loading="lazy" data-testid={`craft-image-${story.id}`} /><div className="craft-card-overlay"><div><p className="text-[10px] uppercase tracking-[0.18em] text-[#f1dfb8]" data-testid={`craft-location-${story.id}`}>0{index + 1} · {story.location}</p><h3 className="mt-2 font-serif text-3xl text-[#f8f1e4]" data-testid={`craft-name-${story.id}`}>{story.name}</h3><p className="mt-2 max-w-sm text-sm leading-6 text-[#f8f1e4]/75" data-testid={`craft-description-${story.id}`}>{story.description}</p></div><ArrowUpRight size={19} className="shrink-0 text-[#c8a45d]" /></div></article>)}</div></section>

        <section id="collection" className="section-shell" data-testid="product-catalog-section"><div className="section-heading"><div><p className="eyebrow text-[#9a7a3e]" data-testid="collection-eyebrow">The house collection</p><h2 className="section-title" data-testid="collection-title">Navidha <em>Silver</em></h2></div><p className="section-intro" data-testid="collection-intro">Contemporary pieces with an Indian point of view. Tarnish-resistant, hand-finished, and made for everyday elegance.</p></div><div className="catalog-toolbar" data-testid="catalog-toolbar"><div className="category-tabs" role="tablist" aria-label="Filter by category" data-testid="category-filter-list">{categories.map((category) => <button type="button" key={category} onClick={() => setActiveCategory(category)} className={`category-tab ${activeCategory === category ? "category-tab-active" : ""}`} role="tab" aria-selected={activeCategory === category} data-testid={`category-filter-${category.toLowerCase().replaceAll(" ", "-")}`}>{category}</button>)}</div><div className="toolbar-controls"><label className="search-field"><Search size={15} /><span className="sr-only">Search collection</span><input type="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search the edit" aria-label="Search the collection" data-testid="catalog-search-input" /></label><label className="select-field"><span className="sr-only">Filter by material</span><select value={activeMaterial} onChange={(event) => setActiveMaterial(event.target.value as "All materials" | Material)} aria-label="Filter by material" data-testid="catalog-material-filter">{materials.map((material) => <option key={material}>{material}</option>)}</select><ChevronDown size={14} /></label><label className="select-field hidden sm:flex"><span className="sr-only">Sort collection</span><select value={sort} onChange={(event) => setSort(event.target.value)} aria-label="Sort collection" data-testid="catalog-sort-filter"><option value="featured">Featured</option><option value="price-low">Price: low to high</option><option value="price-high">Price: high to low</option></select><ChevronDown size={14} /></label></div></div><div className="mb-6 flex items-center justify-between"><p className="text-xs text-[#667383]" data-testid="catalog-result-count">{visibleProducts.length} pieces in this edit</p>{(activeCategory !== "All" || activeMaterial !== "All materials" || search) && <button type="button" onClick={() => { setActiveCategory("All"); setActiveMaterial("All materials"); setSearch(""); }} className="text-[10px] uppercase tracking-[0.18em] text-[#9a7a3e] hover:text-[#14202e]" data-testid="catalog-clear-filters-button">Clear filters</button>}</div>{visibleProducts.length > 0 ? <div className="product-grid" data-testid="product-catalog-grid">{visibleProducts.map((product, index) => <ProductCard key={product.id} product={product} index={index} onSelect={setSelectedProduct} onAdd={addToCart} />)}</div> : <div className="empty-results" data-testid="catalog-empty-state"><Search size={24} /><h3 className="mt-4 font-serif text-2xl">Nothing quite matches.</h3><p className="mt-2 text-sm text-[#667383]">Try another material, category or search phrase.</p></div>}</section>

        <section className="material-banner" data-testid="material-language-section"><div><p className="eyebrow text-[#c8a45d]" data-testid="material-eyebrow">Material language</p><h2 className="mt-4 max-w-2xl font-serif text-4xl leading-tight text-[#f8f1e4] sm:text-6xl" data-testid="material-title">The beauty is in what <em className="text-[#c8a45d]">lasts.</em></h2></div><div className="material-points"><div data-testid="material-point-silver"><ShieldCheck size={17} className="text-[#c8a45d]" /><div><strong>925 silver</strong><span>Tarnish & firescale resistant</span></div></div><div data-testid="material-point-craft"><Sparkles size={17} className="text-[#c8a45d]" /><div><strong>Hand finished</strong><span>Made by a human hand</span></div></div><div data-testid="material-point-pearl"><Gem size={17} className="text-[#c8a45d]" /><div><strong>Natural pearls</strong><span>Each one, beautifully unique</span></div></div></div></section>

        <section id="philosophy" className="section-shell philosophy-section" data-testid="philosophy-section"><div className="philosophy-image"><img src="https://images.pexels.com/photos/10681031/pexels-photo-10681031.jpeg?auto=compress&cs=tinysrgb&q=85" alt="Woman wearing pearl jewelry" loading="lazy" data-testid="philosophy-image" /></div><div className="philosophy-copy"><p className="eyebrow text-[#9a7a3e]" data-testid="philosophy-eyebrow">Our philosophy</p><h2 className="section-title" data-testid="philosophy-title">Crafted in India.<br /><em>Curated by Navidha.</em></h2><p className="mt-7 max-w-lg text-base leading-8 text-[#667383]" data-testid="philosophy-copy">We believe jewelry should feel like a discovery — a small, luminous reminder of where you have been and who you are becoming. Every Navidha edit begins with an Indian craft story and ends with a piece that belongs entirely to you.</p><a href="#collection" className="mt-8 inline-flex items-center gap-3 text-[10px] uppercase tracking-[0.2em] text-[#14202e] transition-colors hover:text-[#9a7a3e]" data-testid="philosophy-collection-link">Discover the house collection <ArrowUpRight size={15} /></a></div></section>

        <section className="waitlist-section" data-testid="waitlist-section"><div className="waitlist-inner"><p className="eyebrow text-[#c8a45d]" data-testid="waitlist-eyebrow">Launching soon</p><h2 className="mt-5 font-serif text-4xl text-[#f8f1e4] sm:text-6xl" data-testid="waitlist-title">Something beautiful<br /><em className="text-[#c8a45d]">is coming.</em></h2><p className="mx-auto mt-6 max-w-md text-sm leading-7 text-[#b8c0c8]" data-testid="waitlist-copy">Be first to know when the Navidha collection opens — and receive a private preview of the pieces made for your story.</p><form className="mx-auto mt-8 flex max-w-md flex-col gap-3 sm:flex-row" onSubmit={(event) => { event.preventDefault(); toast.success("You are on the list", { description: "We will write when the collection opens." }); }} data-testid="waitlist-form"><input type="email" required placeholder="Your email address" aria-label="Email address" className="h-12 flex-1 border border-white/20 bg-white/5 px-4 text-sm text-[#f8f1e4] outline-none placeholder:text-[#b8c0c8]/60 focus:border-[#c8a45d]" data-testid="waitlist-email-input" /><Button type="submit" className="h-12 rounded-none bg-[#c8a45d] px-7 font-sans text-[10px] uppercase tracking-[0.2em] text-[#14202e] hover:bg-[#f1dfb8]" data-testid="waitlist-submit-button">Notify me</Button></form><p className="mt-4 text-[10px] uppercase tracking-[0.16em] text-[#b8c0c8]/60" data-testid="waitlist-note">No spam · unsubscribe anytime</p></div></section>
      </main>

      <footer className="border-t border-[#14202e]/10 bg-[#fbf9f5] px-5 py-10 sm:px-8 lg:px-16" data-testid="site-footer"><div className="mx-auto flex max-w-[1440px] flex-col gap-8 sm:flex-row sm:items-end sm:justify-between"><div><NavidhaLogo compact /><p className="mt-4 max-w-xs text-xs leading-5 text-[#667383]" data-testid="footer-description">Contemporary jewelry rooted in India&apos;s extraordinary craft traditions.</p></div><div className="flex gap-6 text-[10px] uppercase tracking-[0.18em] text-[#667383]"><a href="#collection" className="hover:text-[#9a7a3e]" data-testid="footer-collection-link">Collection</a><a href="#craft" className="hover:text-[#9a7a3e]" data-testid="footer-craft-link">Craft</a><a href="#philosophy" className="hover:text-[#9a7a3e]" data-testid="footer-philosophy-link">Story</a></div><p className="text-[10px] uppercase tracking-[0.14em] text-[#9a7a3e]" data-testid="footer-copyright">© Navidha 2024</p></div></footer>

      <CartDrawer open={cartOpen} items={cartItems} onClose={() => setCartOpen(false)} onQuantityChange={changeQuantity} onRemove={removeItem} />
      {selectedProduct && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#14202e]/70 p-4 backdrop-blur-md" role="dialog" aria-modal="true" aria-label={`${selectedProduct.name} details`} onClick={() => setSelectedProduct(null)} data-testid="product-detail-modal"><div className="product-modal" onClick={(event) => event.stopPropagation()} data-testid="product-detail-content"><button type="button" onClick={() => setSelectedProduct(null)} className="icon-button absolute right-4 top-4 z-10 bg-[#fbf9f5]/80" aria-label="Close product details" data-testid="product-detail-close-button"><X size={18} /></button><div className="grid md:grid-cols-2"><img src={selectedProduct.image} alt={selectedProduct.alt} className="h-full min-h-[330px] w-full object-cover" data-testid="product-detail-image" /><div className="flex flex-col justify-center p-7 sm:p-10"><p className="eyebrow text-[#9a7a3e]" data-testid="product-detail-material">{selectedProduct.material} · {selectedProduct.category}</p><h2 className="mt-4 font-serif text-4xl leading-tight" data-testid="product-detail-name">{selectedProduct.name}</h2><p className="mt-4 text-2xl font-light" data-testid="product-detail-price">{formatPrice(selectedProduct.price)}</p><p className="mt-6 text-sm leading-7 text-[#667383]" data-testid="product-detail-description">{selectedProduct.description}</p><div className="mt-6 border-y border-[#14202e]/10 py-4 text-xs leading-6 text-[#667383]" data-testid="product-detail-specs">{selectedProduct.details}</div><Button type="button" onClick={() => { addToCart(selectedProduct); setSelectedProduct(null); setCartOpen(true); }} className="mt-7 h-12 rounded-none bg-[#14202e] font-sans text-[10px] uppercase tracking-[0.2em] hover:bg-[#c8a45d] hover:text-[#14202e]" data-testid="product-detail-add-button">Add to bag</Button><p className="mt-4 text-center text-[10px] uppercase tracking-[0.16em] text-[#9a7a3e]" data-testid="product-detail-note">Wrapped with care · made to last</p></div></div></div></div>}
    </div>
  );
}
